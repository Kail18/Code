# AI Log

**Question 1**
ChatGPT
**Question 2**
-I uploaded the pdf of the assignment.
-I uploaded the ERD that I had previously done.
-I asked what the multi-table CRUD was referring to.
-I informed it that I am using react as the front end.
-I informed it that I was using mySQL as the database.
-I asked what the ORM/Driver was. I decided on SQLAlchemy.
-I then prompted for starting the front in app using type script.
-I informed it that I will be using bootstrap.
-After using the code generated I noticed there was no functionality so i prompted adding functionality.
-I asked it about the depreciated FormEvent error.
-I asked it to defend itself on why we are using FormEvent since its depreciated.
-I prompted to set up the CSS side.
-I prompted the ability to add and edit finished workouts
-I noticed all the code was in one file for multiple view and components. I prompted for a structured list of folders and files to contain the different components and views.
-After adding the structure in a series of prompts I had it split the code into different views and then give me an updated APP.tsx file.
-I noticed that the table in the finished_workout view was not able to handle a change in window size. I asked for a solution gpt gave a poor solution so I instead prompted it to make a horizontal scroll to the table. This one took multiple prompts as I noticed it kept trying to decrease the minimum window size without adding any scrolling functionality.

**Question 3**

-The AI would generate some code that I would implement if the code was either safe to add or would logically fix the issue.
-The other thing it would provide was information I would read its information. I would also look up the information to myself. Sometimes I would ask it to prove itself.

**Question 4**

I read the code. I then would add the code that made sense to add. I would correct errors and then test out the app to see if it fixed the issue. If it did not fix the issue I revert the changes. After reverting the changes I would do three things on the next prompt. First inform it that I removed the new code. Next inform it that the code did not work. Last try and restate what I was looking for with different words.
